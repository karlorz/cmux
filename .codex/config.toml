# Repo-local CMUX_AUTOPILOT hooks stay inactive during ordinary interactive
# Codex sessions.
#
# `scripts/agent-autopilot.sh` stages `.codex/autopilot-hooks.json` into a
# temporary CODEX_HOME and enables `codex_hooks` only for that launcher path.
