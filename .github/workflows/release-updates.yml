name: Disabled Branch Release Publisher

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain replacement workflow
        run: |
          echo "This workflow is intentionally disabled for the fork release flow."
          echo "Use: Daily Release PR -> merge to main -> Release Tag On Merge -> release-on-tag.yml"
