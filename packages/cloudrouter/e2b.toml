# This is a config for E2B sandbox template.
# You can use template ID (jwxrccum0mglnp704hnk) or template name (cmux-devbox) to create a sandbox:

# Python SDK
# from e2b import Sandbox, AsyncSandbox
# sandbox = Sandbox.create("cmux-devbox") # Sync sandbox
# sandbox = await AsyncSandbox.create("cmux-devbox") # Async sandbox

# JS SDK
# import { Sandbox } from 'e2b'
# const sandbox = await Sandbox.create('cmux-devbox')

template_id = "jwxrccum0mglnp704hnk"
dockerfile = "template/e2b.Dockerfile"
template_name = "cmux-devbox"
start_cmd = "bash /usr/local/bin/start-services.sh"
ready_cmd = "curl -sf http://localhost:39377/health || exit 1"
cpu_count = 4
memory_mb = 8_192
team_id = "aa31b299-02f6-4b38-b9d6-480698e0e377"
